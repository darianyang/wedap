# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # cover the legacy numpy 1.x leg and the modern numpy 2.x leg across a
        # range of supported Python versions
        include:
          - python-version: "3.10"
            numpy: "numpy<2"
          - python-version: "3.12"
            numpy: "numpy>=2"
          - python-version: "3.13"
            numpy: "numpy>=2"

    steps:
    - uses: actions/checkout@v7
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
### tests using conda
#    - name: Add conda to system path
#      run: |
#        # $CONDA is an environment variable pointing to the root of the miniconda directory
#        echo $CONDA/bin >> $GITHUB_PATH
#   - name: Install dependencies
#     run: |
#       conda config --add channels conda-forge
#       conda env update --file requirements.txt --name base
#   - name: Lint with flake8
#     run: |
#       conda install flake8
#       # stop the build if there are Python syntax errors or undefined names
#       flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
#       # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
#       flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
#   - name: Test with pytest
#     run: |
#       conda install pytest
#       pytest wedap/tests
### tests using pip
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # pin numpy to the matrix leg (1.x or 2.x) before installing the package
        pip install "${{ matrix.numpy }}"
        pip install .
        pip install flake8 pytest pytest-cov
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      run: |
        pytest wedap/tests/
        pytest mdap/tests/
        pytest wekap/tests/
